[package]
name = "melosim-app"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "melosim-app"
path = "src/main.rs"

[dependencies]
melosim = { path = ".." }
inventory = "0.3"
three-d = { version = "0.19.0", features = ["egui-gui"] }
