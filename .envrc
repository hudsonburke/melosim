# direnv — automatically sets env vars when entering this directory.
# Install: https://direnv.net
# Usage:   direnv allow  (once, to whitelist this file)

export MUJOCO_DOWNLOAD_DIR="$PWD/.mujoco"
export LD_LIBRARY_PATH="$PWD/.mujoco/mujoco-3.9.0/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
